---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: home-assistant
  namespace: home-assistant
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`home.home`)
      kind: Rule
      services:
        - name: home-assistant
          port: 8123
